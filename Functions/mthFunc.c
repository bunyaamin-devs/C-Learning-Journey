#include <stdio.h>
#include <math.h>

int main(){

    float x = 45;
    float y = 0;

    //y = sqrt(x);
    //y = pow(x, 9);
    // y = round(x);
    // y = ceil(x);
    // y = floor(x);
    // y = abs(x);
    // y = log(x);
    // y = cos(x);
    y = sin(x);
    // y = tan(x);

    printf("%f", y);

    return 0;
}