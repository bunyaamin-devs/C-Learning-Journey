#include <stdio.h>

int main(){

    int x = 8;
    int y = 3;
    int z = 0;

    // z = x + y;
    // z = x - y;
    // z = x * y;
    // z = x / y;
    // z = x % y;
    
    z = x + y;
    z *= 2;
    

    printf("%d", z);

    return 0;
}